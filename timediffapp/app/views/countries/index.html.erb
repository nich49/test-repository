<h1>トップページ</h1>

<p> 二つの国を選択してください</p>
<p>
  <select id="country_base">
    <% @countries.each do |country| %>
      <option value="<%= country.id %>">
        <%= country.title %>
      </option>
    <% end %>
  </select>
</p>

<p>
  <select id="country_target">
    <% @countries.each do |country| %>
      <option value="<%= country.id %>">
        <%= country.title %>
      </option>
    <% end %>
  </select>
</p>

<input type="button" value="test" />